import React, { Component } from "react";
import "./Input.css";

const Input = props => {
    return (
        <h1 className="Input">
            {this.props.text}
        </h1>
    );
  }

export default Input;
